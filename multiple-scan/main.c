#include <stdio.h>

int main()
{
   int n; 
scanf("%d", &n); 
printf("Enter number in %d subject one by one \n",n);
int arr[n]; 
for (int i = 0 ; i < n; ++i) 

    scanf("%d", &arr[i]); 
    return 0;
}
